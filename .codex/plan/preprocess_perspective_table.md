# 任务：preprocess 透视与表格线抑制

## 上下文
- 预处理需在旋转前加入透视校正，但避免表格内框被当作外框
- 电子版发票可能无外框，需保守跳过透视
- 输出方向 scores 与旋转 debug

## 计划
1. preprocess.py: 加入表格线抑制（自适应阈值 + 形态学）并在边缘检测时去除线条
2. preprocess.py: 增加透视可信度判断（角点命中 >= 2），不可信则跳过
3. preprocess.py: 记录透视跳过原因 debug 日志
4. tests: 增加表格内框不裁剪用例
5. README: 简要记录变更
